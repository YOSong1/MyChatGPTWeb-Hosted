# MyChatGPTWeb 설계서 (plan1)

> 학생에게 발급한 OpenAI API Key만 입력하면 chatgpt.com과 비슷한 대화형 웹 페이지를 바로 사용할 수 있게 하는 경량 웹 앱.

작성일: 2026-09-09
버전: v1 (초안)

---

## 1. 목표와 범위

### 1.1 목표
- 학생이 **API Key 하나만 입력**하면 즉시 대화형 도우미로 사용할 수 있어야 한다.
- 인터페이스는 화려하지 않아도 되지만 **ChatGPT / Claude / Gemini와 비슷한 기본 구조**(좌측 대화 목록, 중앙 메시지 영역, 하단 입력창)를 갖춘다.
- 설치와 운영이 단순해야 한다. 서버 관리 부담을 최소화한다.
- 학생의 키가 **서버에 저장되거나 관리자에게 노출되지 않도록** 한다.

### 1.2 v1 범위 (반드시 포함)
- 최초 실행 시 API Key 입력 화면
- 키 유효성 검사 후 채팅 화면 진입
- 텍스트 질문/응답 (스트리밍 출력)
- 여러 개의 대화(스레드) 생성, 전환, 삭제
- 대화 기록 브라우저 로컬 저장 (새로고침해도 유지)
- 마크다운 렌더링, 코드 블록 하이라이트, 코드 복사 버튼
- 모델 선택 (예: gpt-4o-mini 기본, gpt-4o 선택)
- 키 변경 / 키 삭제(로그아웃)

### 1.3 v1 제외 (추후 확장)
- 회원가입 / 로그인 / 사용자 DB
- 파일 업로드, 이미지 생성, 음성 입력
- 서버 측 대화 저장 및 동기화
- 사용량 통계 대시보드

---

## 2. 아키텍처 선택

### 2.1 후보 비교

| 방식 | 설명 | 장점 | 단점 |
|---|---|---|---|
| A. 순수 프런트엔드 (정적 사이트) | 브라우저에서 OpenAI API를 직접 호출 | 서버 불필요, 배포 매우 쉬움, 키가 서버를 거치지 않음 | 브라우저에 키 보관, 향후 서버 기능 추가 시 구조 변경 필요 |
| B. 프런트엔드 + 얇은 프록시 서버 | 브라우저가 키를 헤더로 보내고 서버가 OpenAI에 중계 | CORS 문제 없음, 나중에 로깅·제한 기능 추가 용이 | 서버 운영 필요, 키가 서버를 경유 |
| C. 풀스택 (로그인 + DB) | 사용자 계정, 서버 저장 | 관리 기능 강력 | v1 요구사항 대비 과함 |

### 2.2 결정: **A 방식(정적 사이트)을 기본으로 하되, B로 전환 가능한 구조**

이유:
- OpenAI API는 브라우저 직접 호출을 허용한다(CORS 지원). 프록시 없이도 동작한다.
- 학생 수가 많아도 서버 비용이 0에 가깝다. GitHub Pages, Netlify, Vercel, 학교 내부 웹 서버 어디에나 올릴 수 있다.
- 키가 관리자 서버를 거치지 않으므로 "학생 키를 관리자가 볼 수 있다"는 오해가 생기지 않는다.
- API 호출 부분을 `services/openai.js` 한 파일로 격리해 두면, 나중에 프록시 서버가 필요할 때 이 파일의 base URL만 바꾸면 된다.

---

## 3. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 언어 | JavaScript (ES Modules) | 빌드 없이 실행 가능, 학생·교사 모두 읽기 쉬움 |
| UI | 순수 HTML + CSS + Vanilla JS | 프레임워크 학습 부담 없음, 파일 3~6개로 완결 |
| 마크다운 | marked (CDN) | 가볍고 안정적 |
| 코드 하이라이트 | highlight.js (CDN) | 언어 자동 감지 |
| XSS 방지 | DOMPurify (CDN) | 마크다운 → HTML 변환 결과 정화 |
| 저장소 | localStorage (설정) + IndexedDB 또는 localStorage (대화) | 서버 없이 영속화 |
| API | OpenAI Chat Completions `/v1/chat/completions` (stream: true) | 가장 널리 문서화됨. 이후 Responses API로 교체 가능 |
| 배포 | 정적 호스팅 (GitHub Pages / 학교 서버 / 로컬 `python -m http.server`) | 서버 코드 없음 |

> 대안: 나중에 컴포넌트가 늘어나면 Vite + React(또는 Svelte)로 옮길 수 있다. v1은 의도적으로 빌드 도구 없이 시작한다.

---

## 4. 화면 흐름

```
[앱 시작]
   │
   ├─ localStorage에 키 없음 ──▶ [키 입력 화면]
   │                                 │ 키 입력 + "연결 확인"
   │                                 │ GET /v1/models 로 검증
   │                                 ├─ 실패 → 오류 메시지 표시, 재입력
   │                                 └─ 성공 → 키 저장 → [채팅 화면]
   │
   └─ 키 있음 ──▶ [채팅 화면]
                       │
                       ├─ 좌측 사이드바: 새 대화 / 대화 목록 / 설정
                       ├─ 중앙: 메시지 영역 (스트리밍 표시)
                       ├─ 하단: 입력창 (Enter 전송, Shift+Enter 줄바꿈)
                       └─ 설정 모달: 모델 선택, 시스템 프롬프트, 키 변경/삭제
```

### 4.1 키 입력 화면 (첫 실행)
- 중앙 정렬 카드 하나.
- 구성 요소
  - 제목: "API Key를 입력하세요"
  - 짧은 안내: "키는 이 브라우저에만 저장되며 서버로 전송되지 않습니다."
  - 비밀번호 형식 입력창 + "보기" 토글
  - "이 브라우저에 기억하기" 체크박스 (기본 체크) → 체크 해제 시 sessionStorage 사용(탭 닫으면 삭제)
  - "연결 확인 후 시작" 버튼
  - 오류 영역: 401(잘못된 키), 429(한도 초과), 네트워크 오류를 사람이 읽을 수 있는 문장으로 표시
- 검증 방식: `GET https://api.openai.com/v1/models` 호출. 200이면 통과. 응답의 모델 목록에서 사용 가능 모델을 골라 설정 기본값으로 사용.

### 4.2 채팅 화면
```
┌────────────┬────────────────────────────────────────────┐
│ + 새 대화   │  [모델: gpt-4o-mini ▼]              [⚙ 설정] │
│            │                                            │
│ 대화 1      │   사용자: 파이썬 리스트 정렬 방법?             │
│ 대화 2  ●   │                                            │
│ 대화 3      │   도우미: 다음과 같이 ... (스트리밍 중 ▍)      │
│            │          ```python ... ```  [복사]           │
│            │                                            │
│            │                                            │
│ ───────────│────────────────────────────────────────────│
│ 키 변경     │  ┌──────────────────────────────┐ [전송]     │
│ 내보내기    │  │ 메시지를 입력하세요...          │ [중지]     │
│            │  └──────────────────────────────┘           │
└────────────┴────────────────────────────────────────────┘
```
- 사이드바는 모바일에서 햄버거 버튼으로 접힘.
- 메시지 영역은 새 토큰이 도착할 때 자동 스크롤. 사용자가 위로 스크롤하면 자동 스크롤 일시 중지.
- 응답 중에는 "전송" 버튼이 "중지"로 바뀌고 AbortController로 취소.
- 마지막 응답에 "다시 생성" 버튼.

### 4.3 설정 모달
- 모델 선택 드롭다운 (검증 시 받은 목록 중 `gpt-` 로 시작하는 것만 노출)
- 시스템 프롬프트 (기본값: "당신은 학생을 돕는 친절한 학습 도우미입니다. 한국어로 답합니다.")
- temperature 슬라이더 (기본 0.7)
- API Key 변경 / 삭제 버튼
- 전체 대화 내보내기(JSON) / 가져오기

---

## 5. 데이터 설계

### 5.1 저장 키 (localStorage)
| 키 | 값 | 비고 |
|---|---|---|
| `mcw.apiKey` | 문자열 | "기억하기" 해제 시 sessionStorage에 저장 |
| `mcw.settings` | `{ model, systemPrompt, temperature }` | JSON |
| `mcw.conversations` | `Conversation[]` | JSON. 용량 초과 대비해 v1.1에서 IndexedDB로 이전 가능 |
| `mcw.activeConversationId` | 문자열 | |

### 5.2 자료 구조
```js
Conversation = {
  id: "uuid",
  title: "첫 질문 앞 30자",
  createdAt: 1725000000000,
  updatedAt: 1725000000000,
  messages: [
    { role: "user" | "assistant", content: "...", createdAt: 1725000000000 }
  ]
}
```
- 시스템 프롬프트는 대화에 저장하지 않고 전송 시 설정에서 읽어 맨 앞에 붙인다.
- 제목은 첫 사용자 메시지에서 자동 생성. 더블클릭으로 수정 가능.

### 5.3 컨텍스트 길이 관리
- 전송 시 최근 N개 메시지만 포함(기본 20개). 설정으로 조정.
- 이후 확장: 토큰 수 추정 후 자르기.

---

## 6. 파일 구조

```
MyChatGPTWeb/
├── index.html            # 단일 진입점. 키 화면과 채팅 화면을 모두 포함하고 JS로 전환
├── css/
│   └── style.css         # 레이아웃, 다크/라이트 테마 변수
├── js/
│   ├── app.js            # 부팅: 키 존재 여부 확인 → 화면 전환
│   ├── storage.js        # localStorage/sessionStorage 읽기·쓰기 래퍼
│   ├── openai.js         # API 호출 (validateKey, streamChat). base URL 상수 한 곳
│   ├── chat.js           # 대화 상태 관리 (생성/삭제/메시지 추가/제목 생성)
│   ├── ui.js             # DOM 렌더링, 이벤트 바인딩, 마크다운 렌더
│   └── utils.js          # uuid, 날짜 포맷, 디바운스
├── plan1.md              # 이 문서
└── README.md             # 배포 및 학생 안내
```

---

## 7. 핵심 동작 상세

### 7.1 키 검증 (`openai.validateKey`)
```
GET https://api.openai.com/v1/models
Authorization: Bearer <key>
```
- 200 → `{ ok: true, models: [...] }`
- 401 → "키가 올바르지 않습니다."
- 429 → "요청 한도를 초과했거나 결제 한도가 없습니다."
- 그 외 / 네트워크 오류 → "연결할 수 없습니다. 인터넷 상태를 확인하세요."

### 7.2 스트리밍 응답 (`openai.streamChat`)
```
POST https://api.openai.com/v1/chat/completions
{ model, messages: [system, ...recent], temperature, stream: true }
```
- `fetch` + `ReadableStream`으로 SSE(`data: {...}` 줄) 파싱.
- 청크마다 `delta.content`를 누적하며 화면 갱신(requestAnimationFrame으로 묶어 과도한 리렌더 방지).
- `[DONE]` 수신 시 종료. 최종 텍스트를 대화에 저장.
- AbortController로 중지. 중지 시점까지의 텍스트는 보존.

### 7.3 오류 처리
- 응답 중 오류 발생 시 assistant 말풍선에 오류 문구 + "다시 시도" 버튼.
- 키 만료(401)가 채팅 중 발생하면 키 입력 화면으로 되돌리고 대화는 보존.

### 7.4 렌더링 보안
- assistant 출력은 `marked.parse()` → `DOMPurify.sanitize()` 순으로 처리 후 innerHTML.
- 사용자 입력은 textContent로만 삽입.

---

## 8. 보안 및 운영 고려사항

- **키 보관 위치**: 브라우저 저장소. 공용 PC에서는 "기억하기"를 끄도록 안내. 설정에 "키 삭제" 버튼을 눈에 띄게 배치.
- **HTTPS 필수**: 배포 도메인은 반드시 HTTPS. HTTP에서는 키가 평문으로 노출될 수 있음을 README에 명시.
- **키 발급 시 권장**: 관리자는 OpenAI 대시보드에서 학생별 프로젝트 키를 만들고 월 사용 한도를 설정. 이 앱은 한도 관리를 하지 않는다.
- **레이트 리밋 표시**: 429 응답의 `retry-after` 헤더를 읽어 안내.
- **프록시 전환 시나리오**: 학교 네트워크에서 `api.openai.com`이 차단되거나 사용량 로깅이 필요해지면 `openai.js`의 `BASE_URL`을 자체 프록시로 교체하고, 프록시는 `Authorization` 헤더를 그대로 전달하도록 구현(별도 문서 plan2에서 다룸).

---

## 9. 개발 단계 (진행 방향)

| 단계 | 산출물 | 완료 기준 |
|---|---|---|
| 1. 뼈대 | `index.html`, `style.css`, 두 화면 레이아웃 | 키 화면과 채팅 화면이 버튼으로 전환됨 |
| 2. 키 검증 | `storage.js`, `openai.validateKey` | 실제 키로 검증 성공/실패 메시지가 정확히 표시됨 |
| 3. 단발 대화 | `openai.streamChat`, 메시지 표시 | 질문하면 스트리밍으로 답이 나옴 |
| 4. 대화 관리 | `chat.js`, 사이드바 | 새 대화/전환/삭제, 새로고침 후 유지 |
| 5. 렌더링 품질 | marked + highlight.js + DOMPurify, 복사 버튼 | 코드 블록이 보기 좋게 나오고 복사됨 |
| 6. 설정 | 설정 모달, 모델 선택, 시스템 프롬프트 | 모델 바꾸면 요청에 반영됨 |
| 7. 마무리 | 반응형, 다크 모드, 중지/재생성, 내보내기 | 휴대폰에서 사용 가능 |
| 8. 배포 | README, 정적 호스팅 | 학생에게 URL만 전달하면 동작 |

각 단계는 독립적으로 동작 확인이 가능하도록 한다. 3단계까지 끝나면 이미 학생에게 시험 배포가 가능하다.

---

## 10. 테스트 체크리스트

- [ ] 잘못된 키 입력 시 명확한 오류 문구
- [ ] 올바른 키 입력 후 새로고침해도 채팅 화면 유지
- [ ] "기억하기" 해제 시 탭을 닫으면 키가 사라짐
- [ ] 긴 답변 스트리밍 중 중지 버튼 동작
- [ ] 코드 블록 복사 버튼 동작
- [ ] 대화 10개 이상 생성 후 전환·삭제 정상
- [ ] 모바일 폭(375px)에서 사이드바 접힘 및 입력 가능
- [ ] `<script>` 를 포함한 답변이 실행되지 않고 텍스트로 표시됨
- [ ] 네트워크 끊김 상태에서 전송 시 오류 안내 후 복구

---

## 11. 확장 로드맵 (v2 이후)

1. **프록시 서버 옵션**: 사용량 로깅, 학급별 한도, 키를 서버에서 관리하는 모드 (plan2.md)
2. **Responses API 전환**: 도구 호출, 웹 검색 등 신기능 대응
3. **파일/이미지 첨부**: 이미지 질문, PDF 요약
4. **대화 공유**: 읽기 전용 링크 생성
5. **프리셋 도우미**: "영어 회화 연습", "수학 풀이 검토" 같은 시스템 프롬프트 템플릿
6. **다중 제공자**: 같은 UI로 Gemini, Claude 키도 사용 가능하게 `services/` 계층 추상화

---

## 12. 열린 질문 (사용자 확인 필요)

1. 학생들이 접속할 환경은 학교 PC인가, 개인 기기인가? (공용 PC 비중이 높으면 "기억하기" 기본값을 끄는 편이 안전)
2. `api.openai.com` 직접 접속이 학교 네트워크에서 허용되는가? (차단 시 처음부터 B 방식으로 가야 함)
3. 기본 모델을 무엇으로 할지 (비용 고려 시 gpt-4o-mini 권장)
4. 대화 기록을 관리자가 확인해야 하는 요구가 있는가? (있다면 v1부터 서버 저장 필요)
